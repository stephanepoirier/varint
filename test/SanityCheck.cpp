/**
 * @file SanityCheck.cpp
 *
 * Simple test to ensure that test framework is properly setted up.
 */

 #include "gtest/gtest.h"

/**
 * @test Simple test to ensure that test framework is properly setted up.
 */
TEST(SanityCheck, CheckIntrinsics) {
  ASSERT_TRUE(true);
}
